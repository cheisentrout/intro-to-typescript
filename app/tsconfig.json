{
    "extends": "../tsconfig.base",
    "compilerOptions": {
        "removeComments": true,
    },
    // "glob" - an array of what files to include... not totally clear on what the asterisks mean:
    "include": [
        "./**/*"
    ]
}